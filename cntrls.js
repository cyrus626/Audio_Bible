//controls
function gen(records) {
    var records;
    var emlt = "<h3>Genesis Chapter " + records + "</h3>"
        + "<audio controls autoplay title= 'Gnesis " + records +
        "'><source src='data/genesis/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function exo(records) {
    var records;
    var emlt = "<h3> Exodus Chapter " + records + "</h3>" +
        "<audio controls autoplay title= 'Exodus " + records +
        "'><source src='data/exodus/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function lev(records) {
    var records;
    var emlt = "<h3> Leviticus Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Leviticus " + records +
        "'><source src='data/leviticus/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function num(records) {
    var records;
    var emlt = "<h3> Numbers Chapter " + records + "</h3>" +
        "<audio controls autoplay title = ' Numbers " + records +
        "'><source src='data/numbers/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function deut(records) {
    var records;
    var emlt = "<h3> Deuteronomy Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Deuteronomy " + records +
        "'><source src='data/deuteronomy/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function josh(records) {
    var records;
    var emlt = "<h3> Joshua Chapter " + records + "</h3>" +
        "<audio controls autoplay title= 'Joshua " + records +
        "'><source src='data/joshua/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function jud(records) {
    var records;
    var emlt = "<h3> Judges Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Judges " + records +
        "'><source src='data/judges/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function ruth(records) {
    var records;
    var emlt = "<h3> Ruth Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Ruth " + records +
        "'><source src='data/ruth/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function sam1(records) {
    var records;
    var emlt = "<h3> 1st Samuel Chapter " + records + "</h3>" +
        "<audio controls autoplay title = ' 1st Samuel " + records +
        "'><source src='data/samuel1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function sam2(records) {
    var records;
    var emlt = "<h3> 2nd Samuel Chapter " + records + "</h3>" +
        "<audio controls autoplay title = ' 2nd Samuel " + records +
        "'><source src='data/samuel2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function king1(records) {
    var records;
    var emlt = "<h3> 1st Kings Chapter " + records + "</h3>" +
        "<audio controls autoplay title = ' 1st Kings " + records +
        "'><source src='data/kings1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function king2(records) {
    var records;
    var emlt = "<h3> 2nd Kings Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd Kings " + records +
        "'><source src='data/kings2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function cron1(records) {
    var records;
    var emlt = "<h3> 1st Chronicles Chapter " + records + "</h3>" +
        "<audio controls autoplay title = ' 1st Chronicles " + records +
        "'><source src='data/chronicles1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function cron2(records) {
    var records;
    var emlt = "<h3> 2nd Chronicles Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd Chronicles " + records +
        "'><source src='data/chronicles2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function job(records) {
    var records;
    var emlt = "<h3> Job Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Job " + records +
        "'><source src='data/job/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function ezra(records) {
    var records;
    var emlt = "<h3> Ezra Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Ezra " + records +
        "'><source src='data/ezra/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function neh(records) {
    var records;
    var emlt = "<h3> Nehimiah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Nehimiah " + records +
        "'><source src='data/nehemiah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function esth(records) {
    var records;
    var emlt = "<h3> Esther Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Esther " + records +
        "'><source src='data/esther/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function psalm(records) {
    var records;
    var emlt = "<h3> Psalms " + records + "</h3>" +
        "<audio controls autoplay title = ' Psalms " + records +
        "'><source src='data/psalms/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function prov(records) {
    var records;
    var emlt = "<h3> Proverbs Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Proverbs " + records +
        "'><source src='data/proverbs/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function ecle(records) {
    var records;
    var emlt = "<h3> Ecclesiastes Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Ecclesiastes " + records +
        "'><source src='data/ecclesiastes/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function sos(records) {
    var records;
    var emlt = "<h3> Songs of Solomon Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Songs of Solomon " + records +
        "'><source src='data/songofsolomon/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function isa(records) {
    var records;
    var emlt = "<h3> Isaiah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Isaiah " + records +
        "'><source src='data/isaiah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function jerry(records) {
    var records;
    var emlt = "<h3> Jeremiah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Jeremiah " + records +
        "'><source src='data/jeremiah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function lam(records) {
    var records;
    var emlt = "<h3> Lamentations Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Lamentations " + records +
        "'><source src='data/lamentations/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function eze(records) {
    var records;
    var emlt = "<h3> Ezekiel Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Ezekiel " + records +
        "'><source src='data/ezekiel/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function dan(records) {
    var records;
    var emlt = "<h3> Daniel Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Daniel " + records +
        "'><source src='data/daniel/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function hos(records) {
    var records;
    var emlt = "<h3> Hosea Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Hosea " + records +
        "'><source src='data/hosea/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function joel(records) {
    var records;
    var emlt = "<h3> Joel Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Joel " + records +
        "'><source src='data/joel/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function amos(records) {
    var records;
    var emlt = "<h3> Amos Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Amos " + records +
        "'><source src='data/amos/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function oba(records) {
    var records;
    var emlt = "<h3> Amos Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Obadiah " + records +
        "'><source src='data/obadiah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function joh(records) {
    var records;
    var emlt = "<h3> Jonah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Jonah " + records +
        "'><source src='data/jonah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function mic(records) {
    var records;
    var emlt = "<h3> Micah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Micah " + records +
        "'><source src='data/micah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function nah(records) {
    var records;
    var emlt = "<h3> Nahum Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Nahum " + records +
        "'><source src='data/nahum/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function hab(records) {
    var records;
    var emlt = "<h3> Habakkuk Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Habakkuk " + records +
        "'><source src='data/habakkuk/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function zep(records) {
    var records;
    var emlt = "<h3> Zephaniah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Zephaniah " + records +
        "'><source src='data/zephaniah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function hag(records) {
    var records;
    var emlt = "<h3> Haggai Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Haggai " + records +
        "'><source src='data/haggai/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function zac(records) {
    var records;
    var emlt = "<h3> Zechariah Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'zechariah " + records +
        "'><source src='data/zechariah/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function mal(records) {
    var records;
    var emlt = "<h3> Malachi Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Malachi " + records +
        "'><source src='data/malachi/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function math(records) {
    var records;
    var emlt = "<h3> Matthew Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Matthew " + records +
        "'><source src='data/matthew/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function mark(records) {
    var records;
    var emlt = "<h3> Mark Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Mark " + records +
        "'><source src='data/mark/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function luke(records) {
    var records;
    var emlt = "<h3> Luke Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Luke " + records +
        "'><source src='data/luke/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function john(records) {
    var records;
    var emlt = "<h3> John Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'John " + records +
        "'><source src='data/john/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function act(records) {
    var records;
    var emlt = "<h3> Acts Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Acts " + records +
        "'><source src='data/acts/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function rom(records) {
    var records;
    var emlt = "<h3> Romans Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Romans " + records +
        "'><source src='data/romans/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function cor1(records) {
    var records;
    var emlt = "<h3> 1st Corinthians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '1st Corinthians " + records +
        "'><source src='data/corinthians1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function cor2(records) {
    var records;
    var emlt = "<h3> 2nd Corinthians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd Corinthians " + records +
        "'><source src='data/corinthians/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function gal(records) {
    var records;
    var emlt = "<h3> Galatians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Galatians " + records +
        "'><source src='data/galatians/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function eph(records) {
    var records;
    var emlt = "<h3> Ephesians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Ephesians " + records +
        "'><source src='data/ephesians/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function phil(records) {
    var records;
    var emlt = "<h3> Philiplians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Philipians " + records +
        "'><source src='data/philipians/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function col(records) {
    var records;
    var emlt = "<h3> Colosians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Colosians " + records +
        "'><source src='data/colosians/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function thes1(records) {
    var records;
    var emlt = "<h3> 1st Thessalonians Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '1st Thessalonians " + records +
        "'><source src='data/thessalonians1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function thes2(records) {
    var records;
    var emlt = "<h3> Mark Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Mark " + records +
        "'><source src='data/thessalonians2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function tim1(records) {
    var records;
    var emlt = "<h3> 1st Timothy Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '1st Timothy " + records +
        "'><source src='data/timothy1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function tim2(records) {
    var records;
    var emlt = "<h3> 2nd Timothy Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd Timothy " + records +
        "'><source src='data/timothy2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function tit(records) {
    var records;
    var emlt = "<h3> Titus Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Titus " + records +
        "'><source src='data/titus/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function phi(records) {
    var records;
    var emlt = "<h3> Philemon Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Philemon " + records +
        "'><source src='data/philemon/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function heb(records) {
    var records;
    var emlt = "<h3> Hebrews Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Hebrews " + records +
        "'><source src='datahebrews/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function jam(records) {
    var records;
    var emlt = "<h3> James Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'James " + records +
        "'><source src='data/james/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function pet1(records) {
    var records;
    var emlt = "<h3> 1st Peter Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '1st Peter " + records +
        "'><source src='data/peter1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function pet2(records) {
    var records;
    var emlt = "<h3> 2nd Peter Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd Peter " + records +
        "'><source src='data/peter2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function joh1(records) {
    var records;
    var emlt = "<h3> 1st John Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '1st John " + records +
        "'><source src='data/john1/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function joh2(records) {
    var records;
    var emlt = "<h3> 2nd John Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '2nd John " + records +
        "'><source src='data/john2/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function joh3(records) {
    var records;
    var emlt = "<h3> 3rd John Chapter " + records + "</h3>" +
        "<audio controls autoplay title = '3rd John " + records +
        "'><source src='data/john3/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function jud(records) {
    var records;
    var emlt = "<h3> Jude Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Jude " + records +
        "'><source src='data/jude/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}
function rev(records) {
    var records;
    var emlt = "<h3> Revelations Chapter " + records + "</h3>" +
        "<audio controls autoplay title = 'Revelations " + records +
        "'><source src='data/mark/" + records + ".mp3'/></audio>";
    document.getElementById("ply").innerHTML = emlt;
}